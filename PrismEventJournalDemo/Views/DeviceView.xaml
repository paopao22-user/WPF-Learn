<UserControl x:Class="PrismEventJournalDemo.Views.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrismEventJournalDemo.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="40" Background="White">

        <TextBlock Text="设备管理"
                   FontSize="30"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <Button Content="PLC-01 模拟温度报警"
                Height="45"
                Margin="0,5"
                HorizontalAlignment="Left"
                Command="{Binding AlarmCommand}"
                CommandParameter="PLC-01"/>

        <Button Content="PLC-02 模拟温度报警"
                Height="45"
                Margin="0,5"
                HorizontalAlignment="Left"
                Command="{Binding AlarmCommand}"
                CommandParameter="PLC-02"/>

    </StackPanel>
</UserControl>
