<Window x:Class="day02_1_Grid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:day02_1_Grid"
        mc:Ignorable="d"
        Title="Grid布局练习" Height="300" Width="500">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 顶部:标题栏,跨3列 -->
        <Label Grid.ColumnSpan="3" Content="Grid布局练习" FontSize="20" Foreground="White" Background="Teal" Padding="10"/>
        
        <!-- 底部：状态栏,跨3列  -->
        <Label Grid.Row="2" Grid.ColumnSpan="3" Content="状态栏:准备就绪" Foreground="White" Background="Gray" Padding="5"/>

        <!-- 中间区域:左、中、右三个格子  -->
        <Border Grid.Row="1" Grid.Column="0" Background="LightSalmon" Margin="5">
            <Label Content="左" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Background="LightGreen" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
        
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Content="1" Margin="5"/>
                <Button Grid.Row="0" Grid.Column="1" Content="2" Margin="5"/>
                <Button Grid.Row="0" Grid.Column="2" Content="3" Margin="5"/>
                <Button Grid.Row="1" Grid.Column="0" Content="4" Margin="5"/>
                <Button Grid.Row="1" Grid.Column="1" Content="5" Margin="5"/>
                <Button Grid.Row="1" Grid.Column="2" Content="6" Margin="5"/>
                <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="7" Margin="5"/>
                <Button Grid.Row="2" Grid.Column="1" Content="8" Margin="5"/>
                <Button Grid.Row="2" Grid.Column="2" Content="9" Margin="5"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="2" Background="LightBlue" Margin="5">
            <Label Content="右" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
