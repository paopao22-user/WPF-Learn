<Window x:Class="Command练习.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Command练习"
        xmlns:vm ="clr-namespace:Command练习.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    
        <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="用户名" Width="50" Background="Brown" Margin="5"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="密码" Width="50" Background="Brown" Margin="5"/>
            <TextBox Text="{Binding PassWord, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
        </StackPanel>
        <Button Content="登录" Command="{Binding LoginCommand}" HorizontalAlignment="Left" Width="150" Margin="5"/>
    </StackPanel>
</Window>
