<Window x:Class="PrismRegionDemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismRegionDemo.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="Prism Region练习" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <!-- 左边菜单 -->
        <StackPanel Grid.Column="0"
                    Background="LightGray">

            <Button Content="首页"
                    Height="50"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="HomeView"/>

            <Button Content="设备管理"
                    Height="50"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="DeviceView"/>

            <Button Content="系统设置"
                    Height="50"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="SettingView"/>

        </StackPanel>

        <!-- 右边页面区域 -->
        <ContentControl Grid.Column="1"
                        prism:RegionManager.RegionName="ContentRegion"/>

    </Grid>
</Window>
