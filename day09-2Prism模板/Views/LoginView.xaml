<Window x:Class="Prism模板.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prism模板.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Log.Name}" FontSize="24" Width="80" Height="30" Background="ForestGreen"/>
            <TextBox Text="{Binding Log.Name}" Width="200" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0"/>
        </StackPanel>
        <Button Content="Login" Width="100" Height="30" HorizontalAlignment="Left" Margin="10" Command="{Binding LoginCommand}"/>
    </StackPanel>
</Window>
